export interface PostalCodesAll {
    postalCodeES(value: string): boolean;

    postalCodeUK(value: string): boolean;

    postalCodeUS(value: string): boolean;

    postalCodeIT(value: string): boolean;

    postalCodeDE(value: string): boolean;

    postalCodeNL(value: string): boolean;
}